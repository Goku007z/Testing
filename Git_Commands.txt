	1. Git init - to initialize an empty git repository, you will get a .git folder (empty repo)
	2. Git status - to show the repo status
	3. Git add <filename> - to add file from untracked to staged
	4. Git rm --cache < filename> - to remove from staged to untracked
	5. Git add . -> to add all the files in a staged
	6. Git commit -m "your message" -> from staged to tracked
	7. Git restore <filename> -> to restore a deleted file.
    8. Git rm -> to delete a file

	PAT - Personal Access Token

	2. Git remote add origin <github lnk> to add the github account in local
	3. Git remote -v -> to check the status
	4. Git remote set-url origin <github generated token>@github.com/<push url path> - to grant access to local pc to push the repo in Github at first time
	5. Git push origin master - to push the repo from local to github
    6. Git clone <URL from Github> - to clone from github to local


fork: github-to-github


Branches:
	1. Git branch - to check the number of branch present in repo
	2. Git checkout -B <branch name> - to create a new branch
	3. Git switch <banch name> - to switch to  a different branch
	4. Git log - to check the latest head
	5. Git checkout dev - similar to git switch

Feature branch - we can create from every branch above mentioned
Pull request is a way of merging one branch to anther branch in github
